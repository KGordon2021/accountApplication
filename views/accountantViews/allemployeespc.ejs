<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="../public/styles.css">
    <title>Pay Cycles</title>
</head>

<body>
    <%- include('../partials/nav.ejs') %>
    
    <h1>All Employees for Current Pay Cycle</h1>

    <div>
        
        <% if(pcinfo.length) {
            for(var i = 0; i < pcinfo.length; i++) { 
         %>
     
            <table>
                <thead>
                <tr>
                    <th></th>
                    <th>Employee Department</th>
                    <th>Employee ID: </th>
                    <th>First Name:</th>
                    <th>Last Name:</th>
                    <th> Action </th>
                </tr>
                </thead>
      
                    <tbody>
                        
                        <tr>
                            <td><%= pcinfo[i].emply_dep %></td>
                             <td><%= pcinfo[i].emply_id %> </td>
                            <td><%= pcinfo[i].emply_fn %> </td>
                            <td><%= pcinfo[i].emply_ln %> </td>
                            <td> <a href="/accounts/editpayslip/<%= pcinfo[i].pay_cycle%>/<%= pcinfo[i].employee_id %>/<%= pcinfo[i].id %>">Edit Employee Hours</a></td>
                            <td> <a href="/accounts/viewpayslip/<%= pcinfo[i].pay_cycle%>/<%= pcinfo[i].employee_id %>/<%= pcinfo[i].id %>">Generate Payslip</a></td>
                        </tr>
                    </tbody>
                
            </table>

         <% } %>
         
         <% } else { %>
            <div class="noRecs">
            <p> No current records found</p>
            </div>

        <% } %>
    </div>
    <div class="btn">
        <button>
            <a href="/AccountOff/allemps">Add New Employee Hours</a>
            </button>
        </div>
        
    
</body>
</html>